# ── Resumify Backend — .env.example 
──────────────────────────
# Users supply their own API keys via the frontend UI.
# No server-side AI keys are needed.
# Copy this file to .env if you ever add server-level config.

# Example (not required):
# ALLOWED_ORIGINS=http://localhost:5500,https://yourdomain.vercel.app
